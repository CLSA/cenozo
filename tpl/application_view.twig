{##
 # appointment_view.twig
 # 
 # View appointment details.
 # @author Patrick Emond <emondpd@mcmaster.ca>
 # @see base_view.twig for parameters
 #}
{% extends "base_view.twig" %}

{% block record %}

  {{ parent() }}
  {% from 'macros.twig' import include_list %}
  {{ include_list( 'cohort', cohort_list|default(false), true ) }}
  {{ include_list( 'role', role_list|default(false), true ) }}
  {{ include_list( 'site', site_list|default(false), true ) }}
  {{ include_list( 'region_site', region_site_list|default(false), true ) }}
  {{ include_list( 'jurisdiction', jurisdiction_list|default(false), true ) }}

{% endblock record %}
